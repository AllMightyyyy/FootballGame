// src/theme.js

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#487748",
    },
    secondary: {
      main: "#37003c",
    },
    background: {
      default: "#121212",
    },
  },
  typography: {
   
  },
});

export default theme;
